import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.hotelsearch',
  appName: 'hotel-search-app',
  webDir: 'www'
};

export default config;
